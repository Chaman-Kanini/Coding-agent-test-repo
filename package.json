{
  "name": "application",
  "version": "0.1.0",
  "private": true,
  "description": "Created by Propel provisioning for a repository that had no package manifest.",
  "scripts": {
    "test": "PLAYWRIGHT_JUNIT_OUTPUT_NAME=.propel/junit.xml npx playwright test --reporter=junit; status=$?; cat .propel/junit.xml; exit $status"
  },
  "dependencies": {
    "react": "*",
    "react-dom": "*",
    "react-router": "*",
    "vite": "*",
    "typescript": "*",
    "playwright": "*"
  },
  "devDependencies": {
    "@playwright/test": "1.47.0"
  }
}
